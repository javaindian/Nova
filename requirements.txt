streamlit
pandas
plotly
mysql-connector-python
python-dotenv
# numpy # Usually a dependency of pandas, but can be listed explicitly
# pandas-ta # Will be needed for strategy implementation
# fyers-apiv3 # Or the specific Fyers API library, e.g. fyers-api

# For AI/ML features later (can be commented out initially):
# scikit-learn
# tensorflow # Or tensorflow-lite

# For packaging later:
# PyInstaller # Not a runtime requirement, but for build process.
